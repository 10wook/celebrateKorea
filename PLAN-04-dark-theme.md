## 기능 계획: 어두운 테마 전환(별이 빛나는 모드)

### 목표
- 밤 시간대 자동 다크 테마 적용 및 수동 토글 제공. 별이 반짝이는 배경으로 기념 분위기 강화.

### 사용자 시나리오
- 20:00~06:00 사이 자동으로 어두운 테마가 적용된다(시스템 설정 또는 사용자 선택이 우선).
- 사용자는 헤더 토글 스위치로 테마를 즉시 전환할 수 있다.

### UI/DOM
- `index.html`
  - 테마 토글: `<button id="themeToggle" aria-label="테마 전환"></button>`
  - 별 배경 컨테이너: `<canvas id="stars" aria-hidden="true"></canvas>` 또는 CSS 배경

### 스타일(`styles.css`)
- CSS 변수로 라이트/다크 색상 토큰 정의
- 전환 애니메이션은 짧게(200~300ms)
- 별의 크기/밀도/밝기 변수를 도입하여 기기 성능에 따른 스케일 조정

### 로직(`app.js`)
- `initTheme()`
  - 우선순위: 사용자 저장값(localStorage) > 시스템 설정(prefers-color-scheme) > 시간대(20~06)
  - 전환 시 루트에 클래스 `theme-dark`/`theme-light` 토글
- `initStars()`
  - `<canvas>` 기반 별 생성/렌더(요청 시) 또는 CSS 애니메이션 대안
  - 성능 저하 감지 시 밀도/프레임레이트 감소

### 접근성
- 텍스트 대비 WCAG AA 준수
- 포커스 가시성 유지

### 테스트/검증
- 시간대에 따른 자동 전환 확인
- 토글 사용 후 새로고침해도 사용자 설정 유지 확인

### 수용 기준(AC)
- [x] 자동 전환이 동작하며, 토글이 자동보다 우선한다.
- [x] 별 배경이 성능 저하 없이 동작한다(저사양에서 밀도 축소).
- [x] 3D 씬 하늘과 연동(다크 모드 시 강제 야간, 라이트 모드 시 강제 주간).
- [x] 더블클릭으로 자동 테마 복귀.

