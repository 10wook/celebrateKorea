## CelebrateKorea 기능 구현 계획

### 목표
- 태극기 중심의 단일 페이지에 다음 기능을 추가해 사용자 참여와 기념 의미를 강화
  - 광복절 카운트다운/D-Day
  - 태극기 펄럭임 애니메이션 및 마우스 인터랙션
  - SNS 공유 버튼
  - 야간 자동 어두운 테마(별이 빛나는 모드)와 수동 토글

### 현재 상태 요약
- 단일 페이지 구조(`index.html`, `styles.css`, `app.js`)로 태극기 이미지(`Flag.svg`)만 표시
- 향후 유지보수를 고려해 기능을 `app.js` 내부에서 모듈화된 섹션으로 분리 구현

---

### 기능 요구사항 상세

#### 1) 광복절 카운트다운 / D-Day
- 기준일: 매년 8월 15일 (광복절)
- 로직:
  - 사용자의 현재 날짜 기준으로 다음 광복절(올해 8/15가 지났으면 내년)을 계산
  - 남은 기간을 일/시/분/초 단위로 실시간 표시 (1초 간격 업데이트)
  - 당일(아시아/서울 타임존 기준)에는 카운트다운 대신 메시지 표시
    - 예: "오늘은 제 80주년 광복절입니다 🇰🇷"
    - 주년 계산: 현재연도 − 1945
- 접근성:
  - 라이브 업데이트 영역에 `aria-live="polite"` 적용
  - 스크린리더용 축약/확장 텍스트 제공
- 시간대/정확도:
  - `Intl`과 `Date`를 활용해 Asia/Seoul 기준 계산
  - 새벽 00:00 경계에서 깜빡임 방지(표시값 스냅/보정)

#### 2) 태극기 애니메이션 + 마우스 인터랙션
- 기본:
  - CSS 또는 SVG 필터(`feTurbulence` + `feDisplacementMap`) 기반의 파도(바람) 효과
  - 성능을 위해 `requestAnimationFrame`과 CSS 가속(transform, filter) 위주 사용
- 마우스/포인터 인터랙션:
  - 포인터 이동 각도에 따라 바람 방향/세기 변화
  - `pointermove`를 `requestAnimationFrame` 프록시 및 `throttle`로 최적화
- 접근성/환경설정:
  - `prefers-reduced-motion: reduce` 시 애니메이션 완화 또는 정지
  - 저사양 기기에서는 효과 강도 자동 감소(프레임 드롭 감지 시 강도 축소)

#### 3) SNS 공유 버튼
- 기본 메시지: "광복절을 기념합니다 🇰🇷"
- 동작 우선순위:
  1) Web Share API(`navigator.share`) 지원 시 네이티브 공유 호출
  2) 미지원 시: 링크 복사(Clipboard API) + 안내 토스트
- 메타 태그/미리보기:
  - Open Graph, Twitter Card 메타 태그 추가(제목/설명/이미지)
- 보안/UX:
  - 클릭 피드백(포커스/호버/활성화), 실패 시 재시도 안내

#### 4) 어두운 테마 전환(별이 빛나는 모드)
- 자동 전환:
  - 로컬 시간 20:00~06:00 자동 다크 테마 적용
  - 시스템 설정(`prefers-color-scheme`) 우선 + 사용자 선택 우선순위
- 수동 전환:
  - 헤더 토글 스위치 제공, `localStorage`에 사용자 선택 저장
- 별 배경:
  - CSS 애니메이션 또는 `<canvas>`로 별 점멸 효과 구현
  - 성능 옵션: 저성능 기기에서는 별 밀도 축소
- 대비/가독성:
  - 태극기 시각적 대비 유지(밝기/필터 조정), 텍스트 대비 WCAG 준수

---

### 파일 별 변경 계획
- `index.html`
  - 레이아웃 영역 추가: 헤더(테마 토글/공유), 메인(태극기, 카운트다운), 푸터
  - 접근성/SEO: landmark(`header`, `main`, `footer`), 메타 태그(OG/Twitter), `aria-live`
  - 스크립트 로딩: `defer`로 `app.js` 로드, 필수 DOM 요소에 ID 부여
- `styles.css`
  - 디자인 토큰(CSS 변수) 도입: 색상, 간격, z-index
  - 라이트/다크 테마 변수와 전환 애니메이션, 별 배경 스타일
  - 태극기 펄럭임 기본 스타일 및 `prefers-reduced-motion` 대응
  - 버튼/토글 컴포넌트 스타일(키보드 포커스링 포함)
- `app.js`
  - 모듈형 섹션으로 구성(동일 파일 내 함수 분리)
    - `initCountdown()`, `initFlagAnimation()`, `initShare()`, `initTheme()`
  - 유틸: `getNextGwangbokjeol()`, `formatRemainingTime()`, `getAnniversaryYears()`
  - 상태 저장: 테마(`localStorage.theme`), 리듀스드모션 감지
  - 이벤트: `visibilitychange`로 비활성 시 타이머 일시 정지
- `Flag.svg`
  - 필요 시 필터/그룹 래핑으로 애니메이션 제어가 용이하도록 구조 조정(가급적 CSS만으로 제어)

---

### 데이터/로직 명세
- 주년 계산: `annivYears = targetYear - 1945`
- 남은 시간 포맷: `D일 HH:MM:SS`
- 타임존: Asia/Seoul 기준(표시만 KST 기준으로 고정)

---

### 성능/품질
- 렌더링
  - 애니메이션은 GPU 친화 속성 사용, `will-change` 최소화
  - 이벤트는 `passive`와 `throttle`/`rAF`로 최적화
- 접근성
  - 키보드 포커스 순서/스타일 제공, 버튼에 `aria-label`
  - 라이브 영역에 잦은 갱신은 `polite`로 제한
- 반응형/호환성
  - 모바일 우선, 뷰포트 대응, 고해상도 이미지 최적화
  - Web Share/Clipboard 미지원 브라우저 대비 폴백 제공

---

### 수용 기준(AC)
- 카운트다운이 1초 단위로 갱신되며, 당일 KST 자정 이후 메시지가 자동 전환된다
- 주년 문구가 정확하다(예: 2025-08-15 → 80주년)
- 마우스 이동 시 깃발의 펄럭임 방향/세기가 눈에 띄게 변화한다
- 공유 버튼 클릭 시:
  - 지원 환경: 네이티브 공유 시트 표시
  - 미지원: 링크 복사 완료 안내
- 20:00~06:00 자동 다크 테마, 수동 토글이 자동보다 우선하며 새로고침 후에도 유지된다
- `prefers-reduced-motion`에서 애니메이션이 현저히 줄거나 정지한다

---

### 단계별 일정(권장)
1) 구조/토대(0.5d)
  - HTML 시멘틱 구조/ID, 메타 태그, 기본 스타일 토큰
2) 카운트다운(0.5d)
  - 날짜 계산/표시/접근성
3) 태극기 애니메이션(1d)
  - 기본 펄럭임 + 포인터 인터랙션 + 리듀스드모션
4) SNS 공유(0.5d)
  - Web Share + 클립보드 폴백 + 토스트
5) 다크 테마와 별(1d)
  - 자동/수동 전환, 별 배경 최적화
6) QA/성능 점검(0.5d)
  - 모바일 실기기 테스트, 경계 시나리오, 접근성 점검

---

### 리스크 및 완화
- SVG 필터 성능: 저사양에서 프레임 드롭 → 효과 강도 자동 감소 옵션
- 시간대 이슈: KST 고정 표시 + 시스템 시간 변동 감지 시 1회 재계산
- 공유 API 가용성: 폴백 철저, UI 안내 명확화

---

### 완료 정의(DoD)
- 위 수용 기준 모두 충족, 주요 브라우저/모바일에서 수동 테스트 완료
- ESLint/포맷 통과(해당 시), 콘솔 오류/경고 없음
- 접근성 기본 준수(포커스, 대비, 라이브영역)

---

### 향후 확장 아이디어(옵션)
- 국/영/일 다국어 지원(i18n)
- 기념일 히스토리 타임라인 추가
- PWA 설치 및 오프라인 배경 화면 제공

